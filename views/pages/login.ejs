<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Spotify - Log in</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
</head>
<body class="bg-black flex flex-col items-center justify-center min-h-screen font-sans text-white">
  
  <div class="w-full max-w-[734px] bg-spotify-black rounded-lg p-10 sm:p-20 shadow-2xl flex flex-col items-center">
    <div class="mb-10">
      <i class="ph ph-music-note text-[64px] text-white"></i>
    </div>
    <h1 class="text-3xl font-bold mb-10 text-center tracking-tighter">Log in to Spotify</h1>
    
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="w-full max-w-[324px] bg-[#e91429] text-white text-sm font-bold px-4 py-3 rounded mb-6 flex gap-2 items-center">
        <i class="ph ph-warning-circle text-[20px]"></i>
        <span><%= error %></span>
      </div>
    <% } %>

    <form action="/auth/login" method="POST" class="w-full max-w-[324px] flex flex-col gap-4">
      <div class="flex flex-col gap-2">
        <label for="username" class="text-sm font-bold">Email or username</label>
        <input type="text" id="username" name="username" class="w-full bg-spotify-black border border-[#727272] rounded focus:border-white focus:outline-none focus:ring-1 focus:ring-white px-3 py-3 text-base placeholder-gray-400 hover:border-white transition-colors" placeholder="Email or username" required>
      </div>
      
      <div class="flex flex-col gap-2 mt-2">
        <label for="password" class="text-sm font-bold">Password</label>
        <input type="password" id="password" name="password" class="w-full bg-spotify-black border border-[#727272] rounded focus:border-white focus:outline-none focus:ring-1 focus:ring-white px-3 py-3 text-base placeholder-gray-400 hover:border-white transition-colors" placeholder="Password" required>
      </div>
      
      <button type="submit" class="w-full bg-spotify-green hover:bg-spotify-green-hover text-black font-bold rounded-full px-8 py-3.5 mt-8 transition-transform hover:scale-105 active:scale-100">
        Log In
      </button>
    </form>
    
    <div class="w-full max-w-[324px] h-[1px] bg-spotify-card-hover my-8"></div>
    
    <p class="text-spotify-subdued text-sm font-medium">Don't have an account? <a href="/auth/register" class="text-white hover:text-spotify-green underline hover:no-underline">Sign up for Spotify</a>.</p>
  </div>
</body>
</html>
